dn: olcDatabase=mdb,cn=config
objectClass: olcDatabaseConfig
objectClass: olcMdbConfig
olcDatabase: mdb
olcDbDirectory: ${OPENLDAP_DATA_DIR}
olcSuffix: ${OPENLDAP_BOOTSTRAP_SUFFIX}
olcRootDN: ${OPENLDAP_BOOTSTRAP_DATA_ROOT_DN}
olcRootPW: ${OPENLDAP_BOOTSTRAP_DATA_ROOT_PASSWORD_HASHED}
olcDbMaxSize: ${OPENLDAP_BOOTSTRAP_DATA_DATABASE_MAX_SIZE}
olcDbIndex: objectClass eq
olcDbIndex: uid eq,sub
olcDbIndex: mail eq,sub
olcDbIndex: cn eq,sub
olcDbIndex: sn eq,sub
olcDbIndex: givenName eq,sub
olcDbIndex: member eq
olcDbIndex: uniqueMember eq
olcDbIndex: entryCSN eq
olcDbIndex: entryUUID eq
